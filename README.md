# Multilayer Perceptron Rust

Rust 기반의 MLP(Multilayer Perceptron)를 구현하는 프로젝트입니다.

Keras의 직관적인 사용 방식에서 영감을 받아, 레이어를 쌓고 모델을 컴파일한 뒤 학습하는 형태의 패키지를 만드는 중입니다.

## 목표

- Rust로 MLP 핵심 로직 직접 구현
- Keras 스타일의 간단한 API 제공
- 데이터 전처리, 학습, 예측 흐름 구성
- 추후 재사용 가능한 Rust 패키지로 확장

## 현재 상태

초기 설계 및 구현을 진행 중입니다.
